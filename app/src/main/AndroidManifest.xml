<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Driver_bus_info"
        tools:targetApi="31"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- [런처 하나만 유지] 예: 스플래시만 런처로 -->
        <activity
            android:name=".activity.SplashActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".activity.MainActivity"
            android:exported="false" />

        <activity
            android:name=".activity.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".activity.RegisterPicExActivity"
            android:exported="false"/>

        <activity
            android:name=".activity.RegisterCheckActivity"
            android:exported="false"/>

        <activity
            android:name=".activity.RegisterComActivity"
            android:exported="false"/>

        <activity
            android:name=".activity.UserQuitActivity"
            android:exported="false" />

        <activity
            android:name=".activity.BusListActivity"
            android:exported="false" />

        <activity
            android:name=".activity.DrivingActivity"
            android:exported="false" />

        <activity
            android:name=".activity.BusLogActivity"
            android:exported="false" />

        <activity
            android:name=".activity.AccountEditActivity"
            android:exported="false" />

        <activity
            android:name=".activity.FindAccountActivity"
            android:exported="false" />


        <activity android:name=".activity.NoticeActivity"
            android:exported="false"/>

        <activity android:name=".activity.SettingsActivity"
            android:exported="false"/>

        <activity android:name=".activity.TermsPrivacyActivity"
            android:exported="false"/>

        <activity
            android:name=".activity.InquiryActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>
        <activity
            android:name=".activity.InquiryComposeActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <!-- FileProvider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <meta-data
            android:name="com.naver.maps.map.NCP_KEY_ID"
            android:value="o5lcffxody"/>

        <!-- (선택) 런처 제거 예시: 필요하면 유지하되 MAIN/LAUNCHER는 삭제 -->
        <activity
            android:name=".start.StartLoding"
            android:exported="true" />
    </application>
</manifest>
